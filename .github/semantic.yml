---
# Probot by https://github.com/zeke/semantic-pull-requests

# Always validate the PR title, and ignore the commits
titleOnly: true

# By default types specified in commitizen/conventional-commit-types is used.
# See: commitizen/conventional-commit-types/blob/master/index.json
types:
  - feat      # new feature for the user, not a new feature for build script
  - fix       # bug fix for the user, not a fix to a build script
  - docs      # changes to the documentation
  - style     # formatting, missing semi colons, etc; no prod code change
  - refactor  # refactoring prod code, eg. renaming a variable
  - perf      #
  - test      # adding missing tests, refactoring tests; no prod changes
  - build     #
  - ci        # To work on your CI/CD process
  - chore     # updating grunt tasks etc; no prod code change
  - revert    #
